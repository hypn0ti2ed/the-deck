# Server Configuration
PORT=3000
NODE_ENV=development
APP_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database
DATABASE_PATH=./data/deck.db

# Upload Configuration
UPLOAD_PATH=./data/uploads
MAX_FILE_SIZE=10485760

# Note: Google Calendar and Microsoft Outlook OAuth credentials are configured
# on a per-user basis through the app's Calendar Sync Settings UI.
# No server-side OAuth configuration is required.
